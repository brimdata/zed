# Test that type contexts are properly reset and reusable after stream boundaries

script: |
  zq -b 1 - > s.zng
  zq -z s.zng

inputs:
  - name: stdin
    data: |
      {a:"hello"}
      {b:10}

outputs:
  - name: stdout
    data: |
      {a:"hello"}
      {b:10}
