zed: '*'

input: |
  #separator \x09
  #set_separator	,
  #empty_field	(empty)
  #unset_field	-
  #path	conn
  #fields	_path	foo
  #types	string	string
  conn	yes

output: |
  {_path:"conn" (bstring),foo:"yes" (bstring)} (=0)
