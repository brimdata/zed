zed: '*'

input: |
  #separator \x09
  #set_separator	,
  #empty_field	(empty)
  #unset_field	-
  #fields	f
  #types	set[string]
  -
  (empty)

  dup,dup
  ,
  a,z
  z,a
  -,z,a,,dup,dup

output: |
  {f:null(|[bstring]|)}
  {f:|[]|(|[bstring]|)}
  {f:|["dup"(bstring)]|}
  {f:|[""(bstring)]|}
  {f:|["a"(bstring),"z"(bstring)]|}
  {f:|["a"(bstring),"z"(bstring)]|}
  {f:|[null(bstring),""(bstring),"a"(bstring),"z"(bstring),"dup"(bstring)]|}
