script: zq -f zeek -U in.zson

inputs:
  - name: in.zson
    data: |
      {_path:"",foo:"😁"(bstring)}
      {_path:"magic",foo:"😁"(bstring)}
      {_path:"",foo:"foo😁bar\x00\x01baz"(bstring)}

outputs:
  - name: stdout
    data: |
      #separator \x09
      #set_separator	,
      #empty_field	(empty)
      #unset_field	-
      #fields	foo
      #types	string
      😁
      #path	magic
      😁
      #path	-
      foo😁bar\x00\x01baz
