zed: '*'

input: |
  {_path:"",foo:"😁"(bstring)}
  {_path:"magic",foo:"😁"(bstring)}
  {_path:"",foo:"foo😁bar\x00\x01baz"(bstring)}

output-flags: -f zeek

output: |
  #separator \x09
  #set_separator	,
  #empty_field	(empty)
  #unset_field	-
  #fields	foo
  #types	string
  😁
  #path	magic
  😁
  #path	-
  foo😁bar\x00\x01baz
