script: super query -i json -f zng - | super query -f json -

inputs:
  - name: stdin
    data: |
      {"a": {"b": "1"}}

outputs:
  - name: stdout
    data: |
      {
          "a": {
              "b": "1"
          }
      }
