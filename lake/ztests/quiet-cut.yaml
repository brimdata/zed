script: |
  export SUPER_DB_LAKE=test
  super db init -q
  super db create -q logs
  super db load -q -use logs in.zson
  super db query "from logs | cut notafield"

inputs:
  - name: in.zson
    data: |
      {x:1}
      {x:2}
      {x:3}

outputs:
  - name: stderr
    data: ""
