script: |
  export ZED_LAKE_ROOT=test
  zed lake init -q
  zed lake create -q -p test
  a=$(zed lake add -p test a.zson | head -1 | awk '{print $1}')
  b=$(zed lake add -p test b.zson | head -1 | awk '{print $1}')
  zed lake delete -p test -staging $a $b
  zed lake delete -p test -staging DoesNotExistOtTFSu2ElrcOcHi

inputs:
  - name: a.zson
    data: |
      {a:1}
  - name: b.zson
    data: |
      {b:1}

outputs:
  - name: stderr
    data: |
      DoesNotExistOtTFSu2ElrcOcHi: not found in staging
  - name: stdout
    regexp: |
      \w{27} deleted from staging
      \w{27} deleted from staging
