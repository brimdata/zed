script: |
  export ZED_LAKE_ROOT=test
  zed lake init -q
  zed lake create -q logs
  zed lake drop -f -q logs
  ! zed lake log -use logs@main

inputs:
  - name: babble.zson
    source: ../../testdata/babble.zson

outputs:
  - name: stderr
    data: |
      logs: pool not found
