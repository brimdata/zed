script: |
  export ZED_LAKE_ROOT=test
  zed lake init -q
  zed lake create -q logs
  zed lake load -q -HEAD logs@main babble.zson
  zed lake query "from logs | count()" | zq -z -

inputs:
  - name: babble.zson
    source: ../../testdata/babble.zson

outputs:
  - name: stdout
    data: |
      {count:1000(uint64)}
