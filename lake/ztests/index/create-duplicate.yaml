script: |
  export ZED_LAKE_ROOT=test
  zed lake init -q
  zed lake index create IPs type ip
  ! zed lake index create IPs type ip

outputs:
  - name: stdout
    regexp: |
      IPs
          rule \w{27} type ip
  - name: stderr
    regexp: |
      index rule already exists
