script: |
  source service.sh
  zapi index create IPs type ip
  ! zapi index create IPs type ip

inputs:
  - name: service.sh
    source: ../service.sh

outputs:
  - name: stderr
    regexp: |
      index rule already exists
