script: |
  source service.sh
  zed api create -q -p test
  zed api load -p test in.zson

inputs:
  - name: in.zson
    data: |
      {x:1}
  - name: service.sh
    source: service.sh

outputs:
  - name: stdout
    regexp: |
      \w{27} committed
