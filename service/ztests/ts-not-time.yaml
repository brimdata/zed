script: |
  source service.sh
  zapi create -q test
  zapi load -q -use test@main -
  zapi query -z 'from test'

inputs:
  - name: service.sh
  - name: stdin
    data: &stdin |
      {ts:"A field named ts usually has type time, but this one does not."}

outputs:
  - name: stdout
    data: *stdin
