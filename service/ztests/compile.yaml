script: |
  source service.sh
  curl -d '{"query":"count("}' $ZED_LAKE/compile | zq -z 'cut info' -

inputs:
  - name: service.sh

outputs:
  - name: stdout
    data: |
      {info:[{kind:"Kind",error:"error parsing Zed",pos:6,end:-1}]}
