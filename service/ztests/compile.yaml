script: |
  source service.sh
  curl -d '{"query":"count("}' $SUPER_DB_LAKE/compile | super query -z -c 'cut compilation_errors' -

inputs:
  - name: service.sh

outputs:
  - name: stdout
    data: |
      {compilation_errors:[{Msg:"error parsing SuperPipe",Pos:6,End:-1}]}
