script: |
  source service.sh
  zapi create -q test
  curl -H "Accept: application/json" -d '{"query":"from :pools | cut name"}' http://${ZED_LAKE_HOST}/query

inputs:
  - name: service.sh

outputs:
  - name: stdout
    data: |
      [{"name":"test"}]
