script: |
  zq emptyunion.tzng

inputs:
  - name: emptyunion.tzng
    data: |
        #0:record[a:union[]]
        0:[0:1;]

outputs:
  - name: stderr
    regexp: |
      empty type list in set or union
