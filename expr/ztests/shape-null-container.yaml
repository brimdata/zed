# issues #2853 #2879

zed: |
  put this := shape({
    array:[string],
    record:{c1:int64,c2:bstring,c3:time}
  })

input: |
  {array:null,record:null}

output-flags: -pretty=2

output: |
  {
    array: null (0=([string])),
    record: null (1=({c1:int64,c2:bstring,c3:time}))
  } (=2)
