zed: |
  over tweet_ids with outer=this => (
    ids:=collect(string(this))
    | yield {outer with tweet_ids:join(ids,',')}
  )

input: |
  {name:"bowzer", tweet_ids:[1,2,3]}
  {name:"pinkie", tweet_ids:[4,5]}

output: |
  {name:"bowzer",tweet_ids:"1,2,3"}
  {name:"pinkie",tweet_ids:"4,5"}
