zed: sort host

input: |
  #0:record[host:ip]
  0:[127.0.0.2;]
  #ipaddr=ip
  #1:record[host:ipaddr]
  1:[127.0.0.1;]

output-flags: -f tzng

output: |
  #ipaddr=ip
  #0:record[host:ipaddr]
  0:[127.0.0.1;]
  #1:record[host:ip]
  1:[127.0.0.2;]
