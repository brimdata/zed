script: |
  zdx create -o index -k a in.zng
  zq -t index.zng

inputs:
  - name: in.zng
    data: |
      #0:record[a:string]
      0:[hello;]

outputs:
  - name: stdout
    data: |
      #0:record[key:string]
      0:[hello;]
