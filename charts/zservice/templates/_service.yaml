{{- define "zservice.service" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zservice.fullname" . }}
  labels:
    {{- include "zservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zservice.selectorLabels" . | nindent 4 }}
{{- end -}}
