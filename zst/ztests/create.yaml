script: |
  zq -f zst -o out.zst -
  zq -z -i zst out.zst

inputs:
  - name: stdin
    data: &input |
      {a:"hello",b:1(int32)}
      {a:"world",b:2(int32)}

outputs:
  - name: stdout
    data: *input
