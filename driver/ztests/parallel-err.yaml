script: |
  zq -z 'join a=b' A.zson

inputs:
  - name: A.zson
    data: |
      {a:1 (int32)} (=0)
      {a:2} (0)
      {a:3} (0)

outputs:
  - name: stderr
    data: |
      number of input sources must match number of parallel inputs in zql query
