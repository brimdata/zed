script: |
  zq -I bug.zed -z in.zson

inputs:
  - name: in.zson
    data: |
      {x:1}
  - name: bug.zed
    data: |
      type netx=uint16 ;
      type foo={bar:netx} ;
      *

outputs:
  - name: stdout
    data: |
      {x:1}
