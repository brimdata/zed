script: |
  ! zq -z 'count() by ,x,y' -

inputs:
  - name: stdin
    data: ""

outputs:
  - name: stderr
    data: |
      zq: error parsing Zed at column 12:
      count() by ,x,y
             === ^ ===
