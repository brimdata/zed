script: |
  ! super query -z -c 'count() by ,x,y' -

inputs:
  - name: stdin
    data: ""

outputs:
  - name: stderr
    data: |
      super query: error parsing SuperPipe at line 1, column 12:
      count() by ,x,y
             === ^ ===
