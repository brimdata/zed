script: |
  ! zq -z 'count() by ,x,y' -

inputs:
  - name: stdin
    data: ""

outputs:
  - name: stderr
    data: |
      zq: error parsing Zed (line 1, column 12):
      count() by ,x,y
             === ^ ===
