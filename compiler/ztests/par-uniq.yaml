script: zed compile -C -P 2 -sortKey ts "* | uniq"

outputs:
  - name: stdout
    data: |
      split (
        =>
          filter *
        =>
          filter *
      )
      | merge ts
      | uniq
