script: |
  zed dev compile -C 'traverse foo'
  echo ===
  zed dev compile -C 'traverse foo => (sum(this))'

outputs:
  - name: stdout
    data: |
      traverse foo
      ===
      traverse foo => (
        sum(this)
      )
