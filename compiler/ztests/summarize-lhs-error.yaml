script: |
  ! zc -s 'count() by this[a] := key'
  ! zc -s 'this[a] := count() by key'
  ! zc -s 'this[a] := count()'

outputs:
  - name: stderr
    data: |
      assignment key must be static (line 1, column 12):
      count() by this[a] := key
                 ~~~~~~~
      assignment key must be static (line 1, column 1):
      this[a] := count() by key
      ~~~~~~~
      aggregate output field must be static (line 1, column 1):
      this[a] := count()
      ~~~~~~~
