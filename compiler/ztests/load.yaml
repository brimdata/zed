script: |
  export ZED_LAKE=test
  zed init -q
  zed create -q samples
  zed load -q -use samples babble.zson
  zed create -q Pot
  echo ===
  zed query 'from samples | s=="Potamogalidae-precommissure" | load Pot'

inputs:
  - name: babble.zson
    source: ../../testdata/babble.zson
outputs:
  - name: stdout
    data: |
      ===
      getting empty transaction?