script: zed compile -C -P 2 -sortKey ts "* | sort | uniq"

outputs:
  - name: stdout
    data: |
      split (
        =>
          filter *
        =>
          filter *
      )
      | sort
      | uniq
