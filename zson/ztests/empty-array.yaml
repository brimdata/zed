script: |
  zq -z in.zson

inputs:
  - name: in.zson
    data: |
      {version:[](0=([null]))}
      {version:[1]}

outputs:
  - name: stdout
    data: |
      {version:[](0=([null]))}
      {version:[1]}
