package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintf(os.Stderr, "disabled for now")
	// root.Zed.Add(api.Cmd)
	// args := append([]string{"api"}, os.Args[1:]...)
	// if err := root.Zed.ExecRoot(args); err != nil {
	// fmt.Fprintf(os.Stderr, "%s\n", err)
	// os.Exit(1)
	// }
}
