script: |
  ! super db ls -lake ''
  ! super db serve -lake ''

outputs:
  - name: stderr
    data: |
      lake location must be set (either with the -lake flag or SUPER_DB_LAKE environment variable)
      lake location must be set (either with the -lake flag or SUPER_DB_LAKE environment variable)
