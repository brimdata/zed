script: |
  export ZED_LAKE_ROOT=test
  zed lake init -q
  zed lake index create :ip
  zed lake index create :ip

outputs:
  - name: stderr
    regexp: |
      xrule \w{27} is duplicate of xrule \w{27}
