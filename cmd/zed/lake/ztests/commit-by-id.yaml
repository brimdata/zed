script: |
  export ZED_LAKE_ROOT=test
  zed lake init -q
  zed lake create -q -p POOL
  id=$(zed lake add -p POOL in.zson | head -1 | awk '{print $2}')
  zed lake commit -p POOL $id

inputs:
  - name: in.zson
    data: |
      {x:1}

outputs:
  - name: stdout
    data: |
      commit successful
