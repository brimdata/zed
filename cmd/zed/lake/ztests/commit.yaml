script: |
  export ZED_LAKE_ROOT=test
  zed lake init -q
  zed lake create -q -p test
  zed lake load -p test in.zson

inputs:
  - name: in.zson
    data: |
      {x:1}

outputs:
  - name: stdout
    regexp: |
      \w{27} committed
