script: |
  ! zed ls -lake ''
  ! zed serve -lake ''

outputs:
  - name: stderr
    data: |
      lake location must be set (either with the -lake flag or ZED_LAKE environment variable)
      lake location must be set (either with the -lake flag or ZED_LAKE environment variable)
