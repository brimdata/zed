script: |
  zar import -R logs -empty
  zar index -R logs _path:string :ip
  zar import -R logs data.tzng
  echo ===
  zar find -R logs :ip=127.0.0.1

inputs:
  - name: data.tzng
    data: |
      #0:record[_path:string,ts:time,orig_h:ip]
      0:[conn;1;127.0.0.1;]
      0:[http;1;127.0.0.1;]
      0:[conn;2;127.0.0.2;]
      0:[http;2;127.0.0.2;]

outputs:
  - name: stdout
    data: |
      ===
      hello
