tag: services

env:
  - PG_TEST

script: |
  source startup.sh
  zapi -host $ZQD_HOST new testsp >/dev/null
  zapi -host $ZQD_HOST ls -f zng | zq -f text "pick name" -
  zapi -host $ZQD_HOST rm testsp
  echo ===
  zapi -host $ZQD_HOST ls

inputs:
  - name: startup.sh
    source: startup.sh
  - name: migrations
    symlink: ../migrations

outputs:
  - name: stdout
    data: |
      testsp
      testsp: space removed
      ===
