tag: cluster

script: |
  # Test assumes a remote zqd instance with space "files" is available at 9867
  time zapi -s files get -workers 4 -z 'count()' 2>&1

outputs:
  - name: stdout
    data: |
      {count:12243237 (uint64)} (=0)
